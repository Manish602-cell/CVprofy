// Thin wrapper so project structure matches desired layout.
// Reuse the existing download router so behavior stays exactly the same.
const router = require('./download');

module.exports = router;

